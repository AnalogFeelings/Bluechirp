<Page
    x:Class="Tooter.View.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tooter.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:localcontrols="using:Tooter.LocalControls"
    xmlns:vm="using:Tooter.ViewModel"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <vm:ShellViewModel x:Name="ViewModel"/>
    </Page.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="MenuColumn" Width="60"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Background="{ThemeResource SystemControlAcrylicWindowBrush}">
            <ListView ItemContainerStyle="{ThemeResource ListViewItemRevealBackgroundShowsAboveContentStyle}"
                      IsItemClickEnabled="True">
                <ListViewItem Name="HomeItem" MinWidth="0"
                              HorizontalContentAlignment="Center">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE80F;" />
                </ListViewItem>
            </ListView>
        </ScrollViewer>

        <controls:DropShadowPanel Grid.Column="1">
            <Frame Canvas.ZIndex="20"
                   Name="ContentFrame"/>
        </controls:DropShadowPanel>
    </Grid>
</Page>
